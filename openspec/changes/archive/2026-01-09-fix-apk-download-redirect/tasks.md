# Tasks for `fix-apk-download-redirect`

- [x] 1. **Update HTML:**
    - [x] 1.1. Add the `download` attribute to the `<a>` tag in `docs/index.html`.
- [x] 2. **Validation:**
    - [x] 2.1. Manually verify that clicking the download link in Chrome prompts a download instead of redirecting to Brave.
- [ ] 3. **Commit and Push:**
    - [ ] 3.1. Commit the changes.
    - [ ] 3.2. Push the changes to the remote repository.
