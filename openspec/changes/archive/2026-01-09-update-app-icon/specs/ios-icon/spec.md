## ADDED Requirements

### Requirement: iOS App Icon Display
The application SHALL display the `logo.png` as its app icon on iOS devices.

#### Scenario: App icon on home screen
- **GIVEN** the application is installed on an iOS device.
- **WHEN** the user views the home screen.
- **THEN** the application icon SHALL be the `logo.png` rendered according to iOS icon guidelines.